# ABSOLUTE MINIMAL for Streamlit Cloud - Python 3.13 Compatible
# No version pins to avoid compatibility issues
# For full features, use requirements-full.txt and deploy locally

# Core Data Processing
pandas
numpy

# Visualization (Streamlit auto-installed, Plotly for charts)
plotly

# Web Framework
streamlit

# Utilities
python-dotenv

# Web Scraping (lightweight)
beautifulsoup4
requests
